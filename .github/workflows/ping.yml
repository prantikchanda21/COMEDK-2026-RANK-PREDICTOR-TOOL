name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit URL
        run: |
          curl -s https://comedk-2026-rank-predictor-tool-7fbvvhgzihz4rlybhoue4l.streamlit.app/ > /dev/null || true
          echo "Streamlit app pinged successfully!"
